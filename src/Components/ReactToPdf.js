import React from 'react';

const ReactToPdf = () => {
    return (
        <div>
            {/* {({ toPdf }) => (
                <button onClick={toPdf}>Generate pdf</button>
            )} */}
        </div>
    );
};

export default ReactToPdf;